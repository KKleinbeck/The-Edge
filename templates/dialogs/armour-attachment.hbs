<div class="dialog-form-flex" style="padding-bottom: 15px">
  <select name="selectedArmourId">
    {{#each attachable as |armour|}}
    <option value="{{armour.id}}">
      {{armour.name}} - {{armour.system.structurePoints}}
    </option>
    {{/each}}
  </select>
</div>
