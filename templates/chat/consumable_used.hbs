<h3>{{details.item}} {{genName "consumed"}}</h3>
<div class="centered-row">
  {{detailedLocalisation "chat" "used consumable" details}}
</div>
{{#if hasEffects}}
  <div class="centered-row">
    {{chatName "same name effect"}}
  </div>
{{/if}}